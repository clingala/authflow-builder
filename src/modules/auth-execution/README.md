# Authentication execution module

This module will adapt the validated configuration to Better Auth, provider adapters, verification, and recovery services. It will never accept unvalidated model output or trust tenant/project IDs supplied only by the browser.

