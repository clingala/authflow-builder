# Tool API module

This module will expose the same application services through structured MCP/App SDK tools. Tools will authenticate the caller, validate input, enforce project authorization, and return structured content without secrets.

