# Builder module

This module will own the accessible control surface, selection state, undoable configuration edits, and responsive preview coordination. It will depend on the public auth-config boundary, not persistence details.

